database:
  backend: neo4j
  neo4j:
    uri: ${NEO4J_URI}
    user: ${NEO4J_USER}
    password: ${NEO4J_PASSWORD}
    database: ${NEO4J_DATABASE}
embedding:
  provider: local
  model: all-mpnet-base-v2
  dimension: 768
