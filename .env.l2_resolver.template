# L2 View Resolver Configuration
# Point to remote FalkorDB (Render) where scopelock graph exists
#
# Copy this file to .env.l2_resolver and fill in your actual values

# Environment
ENV=production

# FalkorDB Configuration (Remote)
FALKORDB_MODE=remote
FALKORDB_API_URL=https://mindprotocol.onrender.com/admin/query
FALKORDB_API_KEY=your_falkordb_api_key_here
# Note: GRAPH_NAME extracted from request scope (URL slug), not env var

# Membrane Bus Configuration
MEMBRANE_OBSERVE_URI=ws://localhost:8765/observe
MEMBRANE_INJECT_URI=ws://localhost:8765/inject

# Org Configuration
MEMBRANE_ORG=scopelock

# CPS-1 Economy (Soft-enforce for dev)
CPS1_ENFORCE=false

# Cache Configuration
CACHE_TTL_SECONDS=300
