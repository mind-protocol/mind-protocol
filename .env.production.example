# ==============================================================================
# Mind Protocol - Production Environment Variables
# ==============================================================================
# Copy this file to .env.production and fill in your values
# DO NOT commit .env.production to git (already in .gitignore)

# ------------------------------------------------------------------------------
# Backend (Render) - Set these in Render Dashboard
# ------------------------------------------------------------------------------

# Redis/FalkorDB Connection (auto-populated by Render if using their Redis)
ECONOMY_REDIS_URL=redis://username:password@host:port/0

# WebSocket Server Configuration
WS_PORT=8000

# Dashboard Configuration (not used in backend, but kept for compatibility)
DASHBOARD_PORT=3000

# Hot Reload (MUST be 0 in production)
MP_HOT_RELOAD=0

# Persistence Configuration
MP_PERSIST_ENABLED=1
MP_PERSIST_MIN_BATCH=5
MP_PERSIST_INTERVAL_SEC=5.0

# Economy Runtime Configuration
ECONOMY_ORG_ID=consciousness-infrastructure_mind-protocol
ECONOMY_L2_GRAPH=consciousness-infrastructure_mind-protocol
ECONOMY_THROTTLE_INTERVAL=60
ECONOMY_POLICY_REFRESH=300
ECONOMY_SPEND_ALPHA=0.2
ECONOMY_ROI_ALPHA=0.5
ECONOMY_WALLET_FLOOR_MULT=0.4

# Economy Lane Wallets (JSON string)
# Example: {"engagement": "wallet_address_1", "creation": "wallet_address_2"}
ECONOMY_LANE_WALLETS={}

# ------------------------------------------------------------------------------
# Price Oracle Configuration (Optional - for Solana MIND token price)
# ------------------------------------------------------------------------------

# Solana MIND token mint address
MIND_MINT_ADDRESS=

# Helius API key for Solana RPC (https://helius.xyz)
HELIUS_API_KEY=

# Helius API base URL
HELIUS_BASE_URL=https://api.helius.xyz

# Fallback USD price if oracle fails
MIND_USD_FALLBACK=1.0

# Price cache TTL in seconds
ECONOMY_PRICE_TTL=300

# ------------------------------------------------------------------------------
# UBC Distribution Configuration (Optional - Universal Basic Consciousness)
# ------------------------------------------------------------------------------

# Daily UBC distribution amount
UBC_DAILY=100.0

# Treasury wallet for UBC distribution (Solana address)
UBC_TREASURY_WALLET=

# Citizen wallets for UBC distribution (JSON string)
# Example: {"victor": "wallet_1", "ada": "wallet_2", ...}
UBC_CITIZEN_WALLETS={}

# Cron job offset in seconds (for daily distribution)
UBC_CRON_OFFSET_SECONDS=0

# ------------------------------------------------------------------------------
# Frontend (Vercel) - Set these in Vercel Dashboard
# ------------------------------------------------------------------------------

# Backend WebSocket URL (your Render deployment URL)
NEXT_PUBLIC_WS_URL=https://mind-protocol-backend.onrender.com

# ------------------------------------------------------------------------------
# CORS Configuration (Add to backend code if needed)
# ------------------------------------------------------------------------------

# Allowed origins for WebSocket connections (Vercel frontend URL)
# ALLOWED_ORIGINS=https://your-app.vercel.app

# ------------------------------------------------------------------------------
# Monitoring & Logging (Optional)
# ------------------------------------------------------------------------------

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Sentry DSN for error tracking (optional)
# SENTRY_DSN=

# ------------------------------------------------------------------------------
# Security Notes
# ------------------------------------------------------------------------------
# 1. Never commit actual credentials to git
# 2. Use Render/Vercel environment variable UI to set production values
# 3. Rotate credentials regularly
# 4. Use different values for staging vs production
# 5. FalkorDB connection string includes password - keep secure
